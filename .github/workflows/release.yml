name: Semantic Release & Multi-Platform Build

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      manual_tag:
        description: 'Tag name for manual release (e.g. v0.1.0)'
        required: false
        default: ''

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  semantic-release:
    name: Semantic Release (Analyze & Tag)
    runs-on: ubuntu-latest
    outputs:
      new_release_published: ${{ steps.semantic.outputs.new_release_published }}
      new_release_version: ${{ steps.semantic.outputs.new_release_version }}
      new_release_git_tag: ${{ steps.semantic.outputs.new_release_git_tag }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Semantic Release & Plugins
        run: |
          npm install -g \
            semantic-release \
            @semantic-release/commit-analyzer \
            @semantic-release/release-notes-generator \
            @semantic-release/changelog \
            @semantic-release/exec \
            @semantic-release/git \
            @semantic-release/github \
            conventional-changelog-conventionalcommits

      - name: Run Semantic Release
        id: semantic
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npx semantic-release

  build-release-assets:
    name: Build (${{ matrix.platform_name }})
    needs: semantic-release
    if: |
      always() &&
      (needs.semantic-release.outputs.new_release_published == 'true' || github.event.inputs.manual_tag != '')
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # --- Raspberry Pi / Linux Targets ---
          - target: armv7-unknown-linux-gnueabihf
            os: ubuntu-latest
            platform_name: rpi-armv7-linux
            use_cross: true

          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            platform_name: rpi-aarch64-linux
            use_cross: true

          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            platform_name: x86_64-linux
            use_cross: false

          # --- macOS Targets ---
          - target: aarch64-apple-darwin
            os: macos-latest
            platform_name: macos-aarch64-apple-silicon
            use_cross: false

          - target: x86_64-apple-darwin
            os: macos-13
            platform_name: macos-x86_64-intel
            use_cross: false

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Determine Release Tag
        id: tag
        run: |
          TAG="${{ needs.semantic-release.outputs.new_release_git_tag }}"
          if [ -z "$TAG" ]; then
            TAG="${{ github.event.inputs.manual_tag }}"
          fi
          if [ -z "$TAG" ]; then
            TAG="v$(cargo read-manifest | jq -r .version 2>/dev/null || echo '0.1.0')"
          fi
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install System Dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential protobuf-compiler pkg-config libssl-dev

      - name: Install System Dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install protobuf cmake

      - name: Install Cross (for Linux cross-compilation)
        if: matrix.use_cross
        run: |
          cargo install cross --git https://github.com/cross-rs/cross

      - name: Build Binaries (Native / Cargo)
        if: "!matrix.use_cross"
        run: |
          cargo build --release --target ${{ matrix.target }} --bin pi_node --bin log_receiver

      - name: Build Binaries (Cross)
        if: matrix.use_cross
        run: |
          cross build --release --target ${{ matrix.target }} --bin pi_node --bin log_receiver

      - name: Package Release Assets
        id: package
        shell: bash
        run: |
          RELEASE_TAG="${{ steps.tag.outputs.tag }}"
          ARCHIVE_NAME="minimum-hw-${{ matrix.platform_name }}-${RELEASE_TAG}"
          ARCHIVE_DIR="dist/${ARCHIVE_NAME}"

          mkdir -p "${ARCHIVE_DIR}"

          BIN_PATH="target/${{ matrix.target }}/release"

          cp "${BIN_PATH}/pi_node" "${ARCHIVE_DIR}/"
          cp "${BIN_PATH}/log_receiver" "${ARCHIVE_DIR}/"
          cp README.md "${ARCHIVE_DIR}/" || true

          cd dist
          tar -czvf "${ARCHIVE_NAME}.tar.gz" "${ARCHIVE_NAME}"
          shasum -a 256 "${ARCHIVE_NAME}.tar.gz" > "${ARCHIVE_NAME}.tar.gz.sha256"

          echo "archive_path=dist/${ARCHIVE_NAME}.tar.gz" >> $GITHUB_OUTPUT
          echo "checksum_path=dist/${ARCHIVE_NAME}.tar.gz.sha256" >> $GITHUB_OUTPUT

      - name: Upload Assets to GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          RELEASE_TAG="${{ steps.tag.outputs.tag }}"
          gh release upload "${RELEASE_TAG}" \
            dist/*.tar.gz \
            dist/*.tar.gz.sha256 \
            --clobber
